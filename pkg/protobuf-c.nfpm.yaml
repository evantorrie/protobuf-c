name:    protobuf-c
version: "{{ .StableStatus.STABLE_GIT_VERSION }}-{{ .StableStatus.STABLE_GIT_BRANCH }}"
arch:    x86_64

description: |
    protobuf-c is a C implementation of protocol-buffers
    This particular package was generated at
    {{ .VolatileStatus.ISO8601_DATE }}.

maintainer: Verizon Media

contents:
  - src: "{{ index .Dependencies `//:protobuf-c.libprotobuf-c.so`}}"
    dst: /usr/lib64/libprotobuf-c.so.1
    file_info:
      mode: 0644
      owner: root
      group: root
  - src: /usr/lib64/libprotobuf-c.so.1
    dst: /usr/lib64/libprotobuf-c.so.1.0.0
    type: symlink
  - src: "{{ index .Dependencies `//:protobuf-c.libprotobuf-c.a`}}"
    dst: /usr/lib64/libprotobuf-c.a